```
USAGE:
    CsprojFrameworkVersions.dll scan [DIRECTORY] [OPTIONS]

ARGUMENTS:
    [DIRECTORY]

OPTIONS:
    -h, --help                                            Prints help information
    -u, --excludeUnreadable <EXCLUDE_UNREADABLE>
    -p, --excludeParameterized <EXCLUDE_PARAMETERIZED>
```
